name: Check backfill progress (read-only)
on: { workflow_dispatch: }
concurrency: { group: check-backfill-progress, cancel-in-progress: true }
jobs:
  check:
    runs-on: ubuntu-latest
    environment: Production
    defaults: { run: { working-directory: backend } }
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with: { node-version: 22, cache: npm, cache-dependency-path: backend/package-lock.json }
      - run: npm ci
      - run: node scripts/check-backfill-progress.mjs
        env: { DATABASE_URL: "${{ secrets.DATABASE_URL }}", NODE_OPTIONS: --dns-result-order=ipv4first }
